# config/

Versioned tuning data stamped on every run.

## How it works

`friction-weights.json` (all friction and decision weights, see docs/FRICTION_MODEL.md), `time.json` (peak/work/morning/off-peak hour multipliers), `common-ui.json` (accessible names of cross-cutting UI such as the cookie banner). Bump the `version` field on any change.
