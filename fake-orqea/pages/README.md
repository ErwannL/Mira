# fake-orqea/pages/

Server-rendered pages of the fake (EN/FR).

## How it works

`route.ts` registers public and signed-in pages; `public.ts` (landing, signup, login, public forms), `board.ts` (boards, board, card, automations, members), `app.ts` (onboarding, calendar, notes, forms, QR, search, settings, billing, checkout).
