# shared/test-helpers/

Fixtures: the real personas, catalogue, weights and time config, loaded once for tests; `vigie.ts` holds the examples of Vigie's contract (scenario, persona set).

## How it works

Test code only.
