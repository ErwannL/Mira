# worker/reports/golden/

Golden report files.

## How it works

`funnel.json` is the funnel report of `sampleRun()` (seed 2024). Regenerate deliberately with `UPDATE_GOLDEN=1 npx vitest run worker/reports` and review the diff.
